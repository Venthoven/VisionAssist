import 'package:flutter/material.dart';

class DetectedObjectBox extends StatelessWidget {
  const DetectedObjectBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}